pipeline {
    agent any

    stages {
        stage('Install Apache') {
            steps {
                sh '''
                sudo su
                apt-get install -y apache2
                '''
            }
        }
    }
}

