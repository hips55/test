pipeline {
    agent any

    stages {
        stage('Install Apache') {
            steps {
                sh '''
                sudo apt-get update
                sudo apt-get install -y apache2
                '''
            }
        }
    }
}

